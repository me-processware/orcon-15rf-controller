# Screenshots

Put PNG screenshots here with these exact filenames so the main README picks
them up:

| Filename | What to capture |
|----------|-----------------|
| `web-control.png`   | The main web control page (`http://<device-ip>/`) — modes, metrics, bypass, AC. |
| `airflow-widget.png`| The airflow widget (`http://<device-ip>/widget.html`) — house animation + button row. |
| `display-4848.png`  | A photo of the Guition 4848S040 touch screen running the UI (work in progress). |

Optional extras you can reference from the README later:

| Filename | What to capture |
|----------|-----------------|
| `debug.png` | The `/debug` page with the 31DA byte rulers. |
| `pairing.png` | The pairing card mid-bind ("Searching…"). |

Tip: for the web pages, a browser window at ~380–520 px wide matches the mobile
layout and crops nicely.
